services:
  client:
    build:
      context: .
      args:
        VITE_API_BASE_URL: https://unprotecting-unmelodically-venita.ngrok-free.dev
        VITE_RAZORPAY_KEY_ID: rzp_test_SdgABcswEEFn48
    container_name: altuva-client
    restart: unless-stopped
    ports:
      - "5173:80"
